<Window x:Class="StatsClient.MVVM.View.OrderScanPickerWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:shell="clr-namespace:System.Windows.Shell;assembly=PresentationFramework"
        Title="Add scan/model"
        Height="420"
        Width="320"
        MinHeight="420"
        MaxHeight="420"
        MinWidth="280"
        WindowStartupLocation="Manual"
        ResizeMode="CanResize"
        ShowInTaskbar="False"
        WindowStyle="None"
        Background="#FFF7F9FC">
    <WindowChrome.WindowChrome>
        <shell:WindowChrome CaptionHeight="0"
                            ResizeBorderThickness="5"
                            CornerRadius="0"
                            GlassFrameThickness="0" />
    </WindowChrome.WindowChrome>

    <Border BorderBrush="#FFBECAD8"
            BorderThickness="1"
            Background="#FFF7F9FC">
        <Grid Margin="10">
            <Grid.RowDefinitions>
                <RowDefinition Height="32" />
                <RowDefinition Height="Auto" />
                <RowDefinition Height="*" />
            </Grid.RowDefinitions>

            <Grid Grid.Row="0"
                  Margin="0,0,0,8">
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="*" />
                    <ColumnDefinition Width="Auto" />
                </Grid.ColumnDefinitions>

                <TextBlock Text="Add scan/model"
                           VerticalAlignment="Center"
                           Foreground="#FF2A3D52"
                           FontWeight="SemiBold"
                           FontSize="13" />

                <Button Grid.Column="1"
                        Width="24"
                        Height="24"
                        Margin="0"
                        Padding="0"
                        Background="Transparent"
                        BorderBrush="Transparent"
                        Foreground="#FF4D5F73"
                        FontSize="14"
                        FontWeight="Bold"
                        Content="✕"
                        Click="CloseButton_Click" />
            </Grid>

            

            <Border Grid.Row="2"
                    Background="White"
                    BorderBrush="#FFCAD5E2"
                    BorderThickness="1"
                    CornerRadius="4">
                <ListView x:Name="listFiles"
                          ItemsSource="{Binding Items}"
                          BorderThickness="0"
                          ScrollViewer.VerticalScrollBarVisibility="Auto"
                          ScrollViewer.HorizontalScrollBarVisibility="Disabled">
                    <ListView.GroupStyle>
                        <GroupStyle>
                            <GroupStyle.HeaderTemplate>
                                <DataTemplate>
                                    <Border Background="#FFE8EFF6"
                                            CornerRadius="3"
                                            Padding="6,3"
                                            Margin="2,8,2,4">
                                        <TextBlock Text="{Binding Name}"
                                                   Foreground="#FF1B2A38"
                                                   FontWeight="SemiBold" />
                                    </Border>
                                </DataTemplate>
                            </GroupStyle.HeaderTemplate>
                        </GroupStyle>
                    </ListView.GroupStyle>

                    <ListView.ItemTemplate>
                        <DataTemplate>
                            <Grid Margin="2,1"
                                  MinHeight="24">
                                <Grid.ColumnDefinitions>
                                    <ColumnDefinition Width="24" />
                                    <ColumnDefinition Width="20" />
                                    <ColumnDefinition Width="*" />
                                </Grid.ColumnDefinitions>

                                <Button Grid.Column="0"
                                        Width="20"
                                        Height="20"
                                        Padding="0"
                                        FontSize="13"
                                        FontWeight="Bold"
                                        BorderBrush="Transparent"
                                        Background="Transparent"
                                        Click="ActionButton_Click">
                                    <Button.Style>
                                        <Style TargetType="Button">
                                            <Setter Property="Visibility" Value="Hidden" />
                                            <Setter Property="Content" Value="+" />
                                            <Setter Property="Foreground" Value="#FF1F9D49" />
                                            <Style.Triggers>
                                                <DataTrigger Binding="{Binding IsLoaded}" Value="True">
                                                    <Setter Property="Content" Value="✕" />
                                                    <Setter Property="Foreground" Value="#FFD64545" />
                                                </DataTrigger>
                                                <DataTrigger Binding="{Binding RelativeSource={RelativeSource AncestorType=ListViewItem}, Path=IsMouseOver}" Value="True">
                                                    <Setter Property="Visibility" Value="Visible" />
                                                </DataTrigger>
                                            </Style.Triggers>
                                        </Style>
                                    </Button.Style>
                                </Button>

                                <TextBlock Grid.Column="1"
                                           Text="✔"
                                           Foreground="#FF1F9D49"
                                           FontWeight="Bold"
                                           VerticalAlignment="Center"
                                           HorizontalAlignment="Center">
                                    <TextBlock.Style>
                                        <Style TargetType="TextBlock">
                                            <Setter Property="Visibility" Value="Collapsed" />
                                            <Style.Triggers>
                                                <DataTrigger Binding="{Binding IsLoaded}" Value="True">
                                                    <Setter Property="Visibility" Value="Visible" />
                                                </DataTrigger>
                                            </Style.Triggers>
                                        </Style>
                                    </TextBlock.Style>
                                </TextBlock>

                                <TextBlock Grid.Column="2"
                                           VerticalAlignment="Center"
                                           TextTrimming="CharacterEllipsis"
                                           ToolTip="{Binding FullPath}"
                                           Text="{Binding DisplayName}">
                                    <TextBlock.Style>
                                        <Style TargetType="TextBlock">
                                            <Setter Property="Foreground" Value="#FF1F2E3D" />
                                            <Setter Property="Opacity" Value="1" />
                                            <Style.Triggers>
                                                <DataTrigger Binding="{Binding IsLoaded}" Value="True">
                                                    <Setter Property="Opacity" Value="0.55" />
                                                </DataTrigger>
                                            </Style.Triggers>
                                        </Style>
                                    </TextBlock.Style>
                                </TextBlock>
                            </Grid>
                        </DataTemplate>
                    </ListView.ItemTemplate>
                </ListView>
            </Border>
        </Grid>
    </Border>
</Window>
